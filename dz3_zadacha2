void main() {
  int j = 0;
  var mass = [12, 23, 65, -12, -4];
  int func(int j, List<int> mass) {
    var mass1 = mass.where((element) => element > 0);
    int sum = mass1.fold(0, (sum0, element) => sum0 + element);
    return sum;
  }
  print(func(j, mass));
}

